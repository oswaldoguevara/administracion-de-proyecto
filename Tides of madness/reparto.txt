  /*
        IEnumerator Juego()
        {
            yield return new WaitForSeconds(1f);

            if (servidor == null && cliente == null)
            {
                mazoJalar.barajear();

            }


            if (tipoJugador == Jugador.Local)
            {
                //Revolver las cartas
                mazoJalar.barajear();

                yield return new WaitForEndOfFrame();

                //que hara el otro
                Accion mov = new Accion();
                mov.tipoMovimiento = Accion.TipoMovimiento.OrdenMazoJalar;
                cliente.enviarAccion(mov);

            }


            if (tipoJugador == Jugador.Otro)
            {
                yield return new WaitUntil(() => cartasBarajeadas);
            }




            yield return new WaitForEndOfFrame();
        }
        */
    /*
IEnumerator RepartirCartas()
{

    //Repartir al jugador
    foreach (Carta x in mazoJalar.ObtenerUltimasCartas(5))
    {
        if (tipoJugador == Jugador.Local)
        {
            DarCartaAJugador(x);
        }
        else
        {
            DarCartaAOponente(x);
        }

        yield return new WaitForSeconds(0.3f);
    }

    //Repartir al oponente
    foreach (Carta x in mazoJalar.ObtenerUltimasCartas(5))
    {
        if (tipoJugador == Jugador.Local)
        {
            DarCartaAOponente(x);
        }
        else
        {
            DarCartaAJugador(x);
        }

        yield return new WaitForSeconds(0.3f);
    }



    yield return new WaitForEndOfFrame();
}

*/